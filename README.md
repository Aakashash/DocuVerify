# DocuVerify
DocuVerify is an intelligent image capture and verification application designed to streamline the loan application process. The app ensures high-quality document and identity image capture using real-time validation checks such as clarity, lighting, alignment, and completeness.


node --version	Check Node.js version
java --version	Check Java version
git --version	Check Git version
adb --version	Check ADB version
adb devices	List connected Android devices
adb devices -l	List devices with detailed info
adb kill-server	Restart ADB server
adb start-server	Start ADB server
adb tcpip 5555	Switch device to wireless mode (USB required first)
adb connect 192.168.x.x:5555	Connect to device wirelessly
npx @react-native-community/cli init AppName	Create a new React Native project
npx react-native run-android	Build and run app on connected device
npx react-native run-android --deviceId ID	Run on a specific device
